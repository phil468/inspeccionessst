import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'pe.vanguardfresh.inspeccionessst',
  appName: 'Inspecciones SST',
  webDir: 'www'
};

export default config;
